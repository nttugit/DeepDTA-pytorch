# Runtime on Cheaha: torch + numpy only. Everything else is optional.
numpy>=1.21
torch>=2.0

# Required only by deepdta_pretrain/ (ESM-2 + ChemBERTa embeddings).
# ChemBERTa ships weights as pytorch_model.bin only, and transformers >= 4.52
# refuses to torch.load .bin files unless torch >= 2.6 (CVE-2025-32434).
# With an older torch, pin transformers==4.51.3.
transformers>=4.30,<5

# Required only by deepdta_pretrain --drug-fingerprint ecfp4.
rdkit>=2023.3

# Optional local extras (plots, nicer progress bars). Do not pip-build these
# on Cheaha login nodes; use conda-forge wheels or skip them.
# pyyaml>=5.4
# tqdm>=4.64
# scikit-learn>=1.0
# pillow>=9.5,<11
# matplotlib>=3.5,<3.10

# Config yaml

YAML files consumed by `run/train.py` and `run/experiment.py`.

| File | Purpose |
| --- | --- |
| `davis.yaml` | Davis (pKd), paper lengths 85 / 1200 |
| `kiba.yaml` | KIBA, paper lengths 100 / 1000 |
| `custom.yaml` | User dataset under `data/custom/` |
| `quick.yaml` | 2-epoch smoke test, no grid |
